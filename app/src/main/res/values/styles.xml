<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeBackground">@color/colorPrimary</item>
        <item name="actionBarPopupTheme">@style/ActionModeOverflowMenu</item>
    </style>

    <style name="ActionModeOverflowMenu" parent="Widget.AppCompat.ListPopupWindow">
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:background">@color/cardAndDialogColor</item>
    </style>

    <style name="playButtonStyle" parent="AppTheme">
        <item name="android:src">@drawable/ic_play_circle_filled_black_48dp</item>
    </style>

    <style name="responseButtonStyle" parent="AppTheme">
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:padding">10dp</item>
        <item name="android:gravity">center</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:layout_marginRight">5dp</item>
    </style>

    <style name="KeyboardStyle" parent="AppTheme">
        <item name="keyColor">@color/color_4</item>
        <item name="keyPressedColor">@color/colorAccent</item>
        <item name="primaryTextColor">@color/textColorPrimary</item>
        <item name="secondaryTextColor">@color/textColorPrimary</item>
        <item name="keySpacing">2dp</item>
        <item name="keyBorderRadius">3dp</item>
        <item name="popupTextColor">@color/textColorPrimary</item>
        <item name="popupHighlightColor">@color/colorPrimary</item>
    </style>

</resources>
